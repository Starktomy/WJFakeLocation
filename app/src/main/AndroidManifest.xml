<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.steadywj.wjfakelocation">

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/>
    <!-- Android 13+ permissions -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
    
    <!-- 高德地图所需权限 -->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" 
        android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" 
        android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission android:name="android.permission.READ_MEDIA_VIDEO" />
    <uses-permission android:name="android.permission.READ_MEDIA_AUDIO" />
    
    <!-- 基站/WiFi 定位可选权限 -->
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.CHANGE_WIFI_STATE" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.NEARBY_WIFI_DEVICES" 
        android:usesPermissionFlags="neverForLocation"
        tools:targetApi="tiramisu" />

    <application
        android:name=".WJFakeLocationApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.WJFakeLocation"
        android:extractNativeLibs="true"
        tools:targetApi="34">
        <activity
            android:name=".manager.MainActivity"
            android:exported="true"
            android:theme="@style/Theme.WJFakeLocation">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Settings Provider for Xposed hooks IPC -->
        <provider
            android:name=".provider.SettingsProvider"
            android:authorities="com.steadywj.wjfakelocation.provider"
            android:exported="true" />

        <!-- TODO: 请在此处填写你的百度地图和高德地图的 API KEY -->
        <!-- 百度地图 API KEY -->
        <meta-data
            android:name="com.baidu.lbsapi.API_KEY"
            android:value="在这里填入你的百度地图AK" />
        <!-- 高德地图 API KEY -->
        <meta-data
            android:name="com.amap.api.v2.apikey"
            android:value="在这里填入你的高德地图Key" />

        <!-- Whether to be an Xposed module (specify true) -->
        <meta-data
            android:name="xposedmodule"
            android:value="true"/>
        <!-- Introduction to the module (shown in the framework) -->
        <meta-data
            android:name="xposeddescription"
            android:value="WJFakeLocation - 虚拟定位助手" />
        <!-- The minimum supported Api version of the module is usually 54. -->
        <meta-data
            android:name="xposedminversion"
            android:value="93"/>
        <!-- Module Scopes -->
        <meta-data
            android:name="xposedscope"
            android:resource="@array/xposedscope"/>

        <!-- Indicate usage of new XSharedPreferences -->
        <meta-data
            android:name="xposedsharedprefs"
            android:value="true" />

    </application>

</manifest>